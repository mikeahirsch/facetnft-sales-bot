export const facetPortSaleSignature = `event OfferAccepted(bool success, string offerType, address indexed offerer, address indexed buyer, address indexed seller, address recipient, address assetContract, uint256 assetId, uint256 considerationAmount, address considerationToken, bytes16 offerId)`;
